spring:
  application:
    name: telemetry-service
  data:
    mongodb:
      uri: mongodb://mongo:27017/telemetry

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: INFO
    org.hibernate.type: INFO


server:
  port: 8081
  servlet:
    context-path: /telemetry-service
